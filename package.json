{
  "name": "monad-docs-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for Monad RPC documentation",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.3.0"
  }
}
